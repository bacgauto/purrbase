[providers]
node = "18"
python = "3.10"

[phases.setup]
nixPkgs = ["nodejs-18_x", "python310"]

[phases.install]
cmds = ["npm ci", "pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
